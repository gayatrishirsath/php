<?php
  $s1="PHP is server side scripting language and JAVA is client side scripting language";  
  $s2="language";

 if(preg_match("/$s2/",$s2,$k))
    {
      echo("FOUND");
      print_r($k);
     }
  else
  echo("NOT FOUND");

?>
